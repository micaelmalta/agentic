# Server Configuration
PORT=3001
MAX_CONCURRENT_AGENTS=4

# Jira Configuration
JIRA_HOST=your-domain.atlassian.net
JIRA_USERNAME=your-email@example.com
JIRA_API_TOKEN=your-api-token-here
JIRA_BOARD_ID=1
JIRA_POLL_INTERVAL=15000

# Claude CLI Path: 'claude' (resolved via PATH) or full path e.g. /usr/local/bin/claude (recommended for PTY so logs stream)
CLAUDE_CLI_PATH=claude

# Extra working directories for Claude (--add-dir). Comma- or space-separated absolute paths. Optional.
# CLAUDE_ADD_DIR=/path/to/repo,/path/to/libs
